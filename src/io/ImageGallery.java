package io;


public class ImageGallery {

	/*
	 * 
	 * private static final String IMAGES_FOLDER = "resources//images//";
	 * private final Image Peasant; private final Image Knight; private final
	 * Image Archer; private final Image King;
	 * 
	 * private final EnumMap<Square, Image> squareImages;
	 * 
	 * public ImageGallery() throws MapLoaderException { squareImages = new
	 * EnumMap<>(Square.class);
	 * 
	 * try { String filename;
	 * 
	 * // SQUARES for (Square s : Square.values()) {
	 * 
	 * filename = IMAGES_FOLDER + "squarse//" + s.toString().toLowerCase() +
	 * ".png"; squareImages.put(s, ImageIO.read(new File(filename)));
	 * 
	 * }
	 * 
	 * // UNITS filename = IMAGES_FOLDER + "units//"; Peasant = ImageIO.read(new
	 * File(filename + "peasant.png"));
	 * 
	 * 
	 * } catch (IOException e) { throw new
	 * MapLoaderException("Cannot load images:\n" + e.getMessage()); } }
	 */
}
